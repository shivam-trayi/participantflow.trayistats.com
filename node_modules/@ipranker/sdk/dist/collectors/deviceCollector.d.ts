/**
 * Device Collector
 * Collects device and browser information
 */
import type { DeviceData } from '../types';
export declare class DeviceCollector {
    /**
     * Collect device and browser data
     */
    collect(): Promise<DeviceData>;
}
